# Опциональные зависимости этапа 1 (TTS/STT, микрофон, воспроизведение).
# Установка: pip install -r requirements-audio.txt
# или: pip install -e ".[audio]"

-r requirements.txt

numpy>=1.24.0
sounddevice>=0.4.6
pyttsx3>=2.90
faster-whisper>=1.0.0
